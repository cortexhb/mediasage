# Alembic configuration. No database URL here: `env.py` takes it from
# `backend.db.db`, so one setting decides it for the app and for migrations.
# Logging is left to the application; env.py does not call fileConfig.
[alembic]
script_location = backend/migrations
prepend_sys_path = .
version_path_separator = os
